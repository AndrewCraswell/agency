name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

{%- assign exec = dependency_manager | append: ' exec' -%}
{%- if dependency_manager == 'yarn' -%}
{%- assign exec = 'yarn' -%}
{%- endif -%}
[commands]
dev = "{{ exec }} react-router dev"
