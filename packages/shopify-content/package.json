{
  "name": "@repo/shopify-content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Plan and install portable Shopify content manifests without overwriting merchant content.",
  "scripts": {
    "cli": "tsx src/bin.ts",
    "check:types": "tsc --noEmit",
    "lint": "oxlint --max-warnings=0",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@shopify/cli": "^4.6.1",
    "commander": "^15.0.0",
    "tsx": "^4.23.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/oxlint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "catalog:",
    "vitest": "^4.1.10"
  }
}
