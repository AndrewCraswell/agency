import { orderVariables } from "../../../variables/order.variables.ts"

/** The shared sample order covers this notification; override here if it ever needs its own data. */
export const variables = orderVariables
