import { marketingVariables } from "../../../variables/marketing.variables.ts"

/** Vacation delay runs on the shared marketing context; the away dates live in the Liquid body. */
export const variables = marketingVariables
